<UserControl x:Class="MvvmCounter.CounterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MvvmCounter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock FontSize="28">
            <Run Text="Count:"/>
            <Run Text="{Binding Count}" />
        </TextBlock>
        <Button Content="Up" Margin="24,0" Width="80" Height="28" Command="{Binding UpCommand}" Grid.Column="1" VerticalAlignment="Stretch"/>
        <Button Content="Save" Width="80" Height="28" Command="{Binding SaveCommand}" Grid.Column="2" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
